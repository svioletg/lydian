# discord-vc-bot configuration

prefix = "$"
auto-remove = [".m4a", ".mp3", ".mp4", ".ogg", ".opus", ".wav", ".webm"]

[vote-skipping]
enabled = true
threshold-type = "exact"
percentage = 50
exact = 2

[logging]
log-level = "WARNING"
utc = true
